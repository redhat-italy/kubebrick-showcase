apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/git-branch: master
    apps.open-cluster-management.io/git-path: ops/helm/kubebrick-factory
    apps.open-cluster-management.io/reconcile-option: merge
  labels:
    app: kubebrick-factory
  name: kubebrick-factory-subscription-1
  namespace: kubebrick-factory
spec:
  channel: kubebrick-factory
  placement:
    placementRef:
      kind: PlacementRule
      name: kubebrick-factory-placement-1